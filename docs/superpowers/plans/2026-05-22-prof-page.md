# Personal Professional IT Page — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a single-page personal professional website with terminal dark aesthetic, animated particle hero, GSAP scroll animations, and a Formspree contact form, deployed to GitHub Pages with a custom `.me` domain.

**Architecture:** Single `index.html` holds all content. `assets/css/style.css` holds all styles. `assets/js/main.js` holds all behaviour. Libraries loaded via CDN — no build step, no framework.

**Tech Stack:** Vanilla HTML5 / CSS3 / JS (ES2020+), tsParticles 2, Typed.js 2, GSAP 3 + ScrollTrigger, Formspree (free tier), Playwright (tests only), GitHub Pages.

---

## File Map

| File | Purpose |
|---|---|
| `index.html` | Full page structure — all 6 sections, all semantic HTML |
| `assets/css/style.css` | All styles: tokens, reset, layout, components, responsive |
| `assets/js/main.js` | All JS: particles, typewriter, GSAP, ScrollSpy, hamburger, form |
| `tests/e2e/sections.spec.js` | Playwright: all sections present, labels correct |
| `tests/e2e/nav.spec.js` | Playwright: nav links, ScrollSpy active class, hamburger |
| `tests/e2e/contact.spec.js` | Playwright: form fields, submit success/error |
| `playwright.config.js` | Playwright config (serves via `python3 -m http.server`) |
| `package.json` | Playwright dependency only |
| `.gitignore` | Ignores node_modules, test output |
| `CNAME` | Custom domain for GitHub Pages (added in Task 10) |

---

### Task 1: HTML scaffold — full semantic structure

**Files:**
- Create: `index.html`
- Create: `assets/css/style.css`
- Create: `assets/js/main.js`
- Create: `.gitignore`

- [ ] **Step 1: Create `.gitignore`**

```
node_modules/
test-results/
playwright-report/
```

- [ ] **Step 2: Create `assets/css/style.css`** (placeholder — filled in Task 3)

```css
/* styles added in Task 3 */
```

- [ ] **Step 3: Create `assets/js/main.js`** (placeholder — filled in Tasks 4–7)

```js
// behaviour added in Tasks 4-7
```

- [ ] **Step 4: Create `index.html`**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Name — DevOps Engineer</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="assets/css/style.css">
</head>
<body>

  <nav id="nav">
    <div class="nav-brand"><span class="mono">~/</span>username</div>
    <ul class="nav-links">
      <li><a href="#about">About</a></li>
      <li><a href="#skills">Skills</a></li>
      <li><a href="#experience">Experience</a></li>
      <li><a href="#projects">Projects</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
    <button class="hamburger" aria-label="Menu" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>
  </nav>

  <div class="nav-overlay" role="dialog" aria-label="Navigation">
    <a href="#about">About</a>
    <a href="#skills">Skills</a>
    <a href="#experience">Experience</a>
    <a href="#projects">Projects</a>
    <a href="#contact">Contact</a>
  </div>

  <section id="hero">
    <div id="particles"></div>
    <div class="hero-content">
      <p class="hero-prompt"><span class="muted">~/portfolio $</span> <span class="accent">run --intro</span></p>
      <p class="hero-status"><span class="muted">Initializing...</span> <span class="accent">done</span></p>
      <h1 class="hero-name">Your Name</h1>
      <p class="hero-role"><span id="typed"></span><span class="cursor">_</span></p>
      <p class="hero-tagline"><span class="muted"># Building scalable infrastructure and systems that last</span></p>
      <div class="hero-cta">
        <a href="#contact" class="btn btn-primary">./contact.sh</a>
        <a href="assets/resume.pdf" download class="btn btn-secondary">cat resume.pdf</a>
      </div>
    </div>
  </section>

  <section id="about">
    <div class="container">
      <p class="section-label">// ABOUT_ME</p>
      <div class="about-bio">
        <p>I'm a DevOps and platform engineer who builds reliable, scalable infrastructure. I work with Kubernetes, Terraform, and CI/CD pipelines daily — and I reach for C when performance and precision matter at the system level.</p>
      </div>
      <div class="about-stats">
        <div class="stat">
          <span class="stat-value">3+</span>
          <span class="stat-label">YRS EXP</span>
        </div>
        <div class="stat">
          <span class="stat-value">10+</span>
          <span class="stat-label">PROJECTS</span>
        </div>
        <div class="stat">
          <span class="stat-value">5+</span>
          <span class="stat-label">TECHNOLOGIES</span>
        </div>
      </div>
    </div>
  </section>

  <section id="skills">
    <div class="container">
      <p class="section-label">// SKILLS</p>
      <div class="skills-grid">
        <div class="skill-group">
          <p class="skill-category">INFRASTRUCTURE</p>
          <div class="skill-tags">
            <span class="tag tag--primary">Kubernetes</span>
            <span class="tag tag--primary">Docker</span>
            <span class="tag tag--primary">Terraform</span>
            <span class="tag tag--primary">Ansible</span>
            <span class="tag tag--primary">Helm</span>
          </div>
        </div>
        <div class="skill-group">
          <p class="skill-category">CLOUD</p>
          <div class="skill-tags">
            <span class="tag tag--primary">AWS</span>
            <span class="tag tag--primary">GCP</span>
          </div>
        </div>
        <div class="skill-group">
          <p class="skill-category">CI/CD</p>
          <div class="skill-tags">
            <span class="tag tag--primary">GitLab CI</span>
            <span class="tag tag--primary">GitHub Actions</span>
            <span class="tag tag--primary">ArgoCD</span>
          </div>
        </div>
        <div class="skill-group">
          <p class="skill-category">LANGUAGES</p>
          <div class="skill-tags">
            <span class="tag tag--secondary">C</span>
            <span class="tag tag--secondary">Bash</span>
            <span class="tag tag--secondary">Python</span>
          </div>
        </div>
        <div class="skill-group">
          <p class="skill-category">MONITORING</p>
          <div class="skill-tags">
            <span class="tag tag--secondary">Prometheus</span>
            <span class="tag tag--secondary">Grafana</span>
            <span class="tag tag--secondary">ELK</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="experience">
    <div class="container">
      <p class="section-label">// EXPERIENCE</p>
      <div class="timeline">
        <div class="timeline-entry timeline-entry--current">
          <div class="timeline-node"></div>
          <div class="timeline-content">
            <h3 class="timeline-role">DevOps Engineer <span class="muted">@ Company Name</span></h3>
            <p class="timeline-date">2023 – present</p>
            <ul class="timeline-bullets">
              <li>Achievement or responsibility one</li>
              <li>Achievement or responsibility two</li>
            </ul>
          </div>
        </div>
        <div class="timeline-entry">
          <div class="timeline-node"></div>
          <div class="timeline-content">
            <h3 class="timeline-role">Previous Role <span class="muted">@ Company Name</span></h3>
            <p class="timeline-date">2021 – 2023</p>
            <ul class="timeline-bullets">
              <li>Achievement or responsibility one</li>
              <li>Achievement or responsibility two</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="projects">
    <div class="container">
      <p class="section-label">// PROJECTS</p>
      <div class="projects-grid">
        <article class="project-card">
          <h3 class="project-name"><span class="accent">&gt;</span> project-name</h3>
          <p class="project-desc">Short description of the project and what problem it solves.</p>
          <div class="project-tags">
            <span class="tag tag--primary">Go</span>
            <span class="tag tag--primary">Kubernetes</span>
          </div>
          <div class="project-links">
            <a href="#" class="project-link" target="_blank" rel="noopener">GitHub ↗</a>
          </div>
        </article>
        <article class="project-card">
          <h3 class="project-name"><span class="accent">&gt;</span> another-project</h3>
          <p class="project-desc">Short description of the project and what problem it solves.</p>
          <div class="project-tags">
            <span class="tag tag--secondary">C</span>
          </div>
          <div class="project-links">
            <a href="#" class="project-link" target="_blank" rel="noopener">GitHub ↗</a>
          </div>
        </article>
      </div>
    </div>
  </section>

  <section id="contact">
    <div class="container">
      <p class="section-label">// CONTACT</p>
      <div class="contact-grid">
        <div class="contact-links">
          <a href="https://github.com/username" class="contact-link" target="_blank" rel="noopener">
            <span class="contact-label">GitHub</span>
            <span class="contact-url">github.com/username ↗</span>
          </a>
          <a href="https://linkedin.com/in/username" class="contact-link" target="_blank" rel="noopener">
            <span class="contact-label">LinkedIn</span>
            <span class="contact-url">linkedin.com/in/username ↗</span>
          </a>
          <a href="mailto:you@email.com" class="contact-link">
            <span class="contact-label">Email</span>
            <span class="contact-url">you@email.com ↗</span>
          </a>
        </div>
        <form class="contact-form" action="https://formspree.io/f/FORM_ID" method="POST">
          <div class="form-group">
            <input type="text" name="name" placeholder="name" required class="form-input">
          </div>
          <div class="form-group">
            <input type="email" name="email" placeholder="email" required class="form-input">
          </div>
          <div class="form-group">
            <textarea name="message" placeholder="message" required class="form-textarea" rows="4"></textarea>
          </div>
          <button type="submit" class="btn btn-primary">send →</button>
          <p class="form-status" aria-live="polite"></p>
        </form>
      </div>
    </div>
  </section>

  <footer>
    <div class="container">
      <p><span class="mono">~/portfolio $</span> <span class="muted">exit 0</span></p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/tsparticles@2/tsparticles.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/typed.js@2/dist/typed.umd.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3/dist/gsap.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3/dist/ScrollTrigger.min.js"></script>
  <script src="assets/js/main.js"></script>
</body>
</html>
```

- [ ] **Step 5: Verify structure in browser**

```bash
python3 -m http.server 8080
```

Open `http://localhost:8080`. Expected: unstyled but all six section labels visible (`// ABOUT_ME`, `// SKILLS`, etc.). No broken layout errors.

- [ ] **Step 6: Commit**

```bash
git add index.html assets/css/style.css assets/js/main.js .gitignore
git commit -m "feat: add HTML scaffold with full page structure"
```

---

### Task 2: Playwright setup + failing tests

**Files:**
- Create: `package.json`
- Create: `playwright.config.js`
- Create: `tests/e2e/sections.spec.js`
- Create: `tests/e2e/nav.spec.js`
- Create: `tests/e2e/contact.spec.js`

- [ ] **Step 1: Create `package.json`**

```json
{
  "name": "prof-page",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  },
  "scripts": {
    "test": "playwright test"
  }
}
```

- [ ] **Step 2: Install Playwright**

```bash
npm install
npx playwright install chromium
```

Expected: `node_modules/` created, chromium browser binary downloaded.

- [ ] **Step 3: Create `playwright.config.js`**

```js
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',
  use: { baseURL: 'http://localhost:8080' },
  webServer: {
    command: 'python3 -m http.server 8080',
    url: 'http://localhost:8080',
    reuseExistingServer: !process.env.CI,
  },
});
```

- [ ] **Step 4: Create `tests/e2e/sections.spec.js`**

```js
import { test, expect } from '@playwright/test';

test('all six sections are present', async ({ page }) => {
  await page.goto('/');
  for (const id of ['hero', 'about', 'skills', 'experience', 'projects', 'contact']) {
    await expect(page.locator(`#${id}`)).toBeVisible();
  }
});

test('section labels render correctly', async ({ page }) => {
  await page.goto('/');
  await expect(page.locator('#about .section-label')).toHaveText('// ABOUT_ME');
  await expect(page.locator('#skills .section-label')).toHaveText('// SKILLS');
  await expect(page.locator('#experience .section-label')).toHaveText('// EXPERIENCE');
  await expect(page.locator('#projects .section-label')).toHaveText('// PROJECTS');
  await expect(page.locator('#contact .section-label')).toHaveText('// CONTACT');
});

test('skills grid has five groups', async ({ page }) => {
  await page.goto('/');
  await expect(page.locator('.skill-group')).toHaveCount(5);
});

test('timeline has at least two entries', async ({ page }) => {
  await page.goto('/');
  const count = await page.locator('.timeline-entry').count();
  expect(count).toBeGreaterThanOrEqual(2);
});

test('projects grid has at least two cards', async ({ page }) => {
  await page.goto('/');
  const count = await page.locator('.project-card').count();
  expect(count).toBeGreaterThanOrEqual(2);
});
```

- [ ] **Step 5: Create `tests/e2e/nav.spec.js`**

```js
import { test, expect } from '@playwright/test';

test('nav links visible on desktop', async ({ page }) => {
  await page.setViewportSize({ width: 1200, height: 800 });
  await page.goto('/');
  await expect(page.locator('.nav-links')).toBeVisible();
  await expect(page.locator('.nav-links a[href="#about"]')).toBeVisible();
  await expect(page.locator('.nav-links a[href="#contact"]')).toBeVisible();
});

test('hamburger hidden on desktop, visible on mobile', async ({ page }) => {
  await page.setViewportSize({ width: 1200, height: 800 });
  await page.goto('/');
  await expect(page.locator('.hamburger')).toBeHidden();

  await page.setViewportSize({ width: 375, height: 812 });
  await expect(page.locator('.hamburger')).toBeVisible();
});

test('hamburger toggles nav overlay open and closed', async ({ page }) => {
  await page.setViewportSize({ width: 375, height: 812 });
  await page.goto('/');

  const overlay = page.locator('.nav-overlay');
  await expect(overlay).not.toHaveClass(/open/);

  await page.locator('.hamburger').click();
  await expect(overlay).toHaveClass(/open/);

  await page.locator('.hamburger').click();
  await expect(overlay).not.toHaveClass(/open/);
});

test('clicking an overlay link closes the menu', async ({ page }) => {
  await page.setViewportSize({ width: 375, height: 812 });
  await page.goto('/');
  await page.locator('.hamburger').click();
  await page.locator('.nav-overlay a[href="#about"]').click();
  await expect(page.locator('.nav-overlay')).not.toHaveClass(/open/);
});
```

- [ ] **Step 6: Create `tests/e2e/contact.spec.js`**

```js
import { test, expect } from '@playwright/test';

test('contact form has all required fields', async ({ page }) => {
  await page.goto('/');
  await expect(page.locator('.contact-form input[name="name"]')).toBeVisible();
  await expect(page.locator('.contact-form input[name="email"]')).toBeVisible();
  await expect(page.locator('.contact-form textarea[name="message"]')).toBeVisible();
  await expect(page.locator('.contact-form button[type="submit"]')).toBeVisible();
});

test('successful submission shows confirmation and resets the form', async ({ page }) => {
  await page.route('https://formspree.io/**', (route) =>
    route.fulfill({ status: 200, body: JSON.stringify({ ok: true }) })
  );
  await page.goto('/');

  await page.fill('.contact-form input[name="name"]', 'Test User');
  await page.fill('.contact-form input[name="email"]', 'test@example.com');
  await page.fill('.contact-form textarea[name="message"]', 'Hello from Playwright');
  await page.click('.contact-form button[type="submit"]');

  await expect(page.locator('.form-status')).toHaveText('done. message sent.');
  await expect(page.locator('.form-status')).toHaveClass(/form-status--success/);
  await expect(page.locator('.contact-form input[name="name"]')).toHaveValue('');
});

test('failed submission shows error message', async ({ page }) => {
  await page.route('https://formspree.io/**', (route) =>
    route.fulfill({ status: 500, body: 'error' })
  );
  await page.goto('/');

  await page.fill('.contact-form input[name="name"]', 'Test User');
  await page.fill('.contact-form input[name="email"]', 'test@example.com');
  await page.fill('.contact-form textarea[name="message"]', 'Test message');
  await page.click('.contact-form button[type="submit"]');

  await expect(page.locator('.form-status')).toHaveClass(/form-status--error/);
});
```

- [ ] **Step 7: Run tests — expect partial pass**

```bash
npm test
```

Expected:
- `sections.spec.js` — all 5 pass (HTML is already in place)
- `nav.spec.js` — hamburger/overlay tests fail (JS not yet added)
- `contact.spec.js` — submit tests fail (JS not yet added)

- [ ] **Step 8: Commit**

```bash
git add package.json playwright.config.js tests/
git commit -m "test: add Playwright e2e tests for sections, nav, and contact form"
```

---

### Task 3: CSS — complete styles

**Files:**
- Modify: `assets/css/style.css`

- [ ] **Step 1: Replace `assets/css/style.css` with the full stylesheet**

```css
/* ============================================================
   DESIGN TOKENS
   ============================================================ */
:root {
  --bg:          #0f0f0f;
  --bg-card:     #111111;
  --bg-input:    #0d0d0d;
  --accent:      #00ff88;
  --accent-glow: rgba(0, 255, 136, 0.12);
  --accent-dim:  rgba(0, 255, 136, 0.15);
  --text:        #e2e2e2;
  --muted:       #666666;
  --muted2:      #555555;
  --border:      #1e1e1e;
  --border-dim:  #222222;
  --font-mono:   'JetBrains Mono', monospace;
  --font-body:   -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  --nav-h:       60px;
  --max-w:       900px;
  --ease:        0.25s ease;
}

/* ============================================================
   RESET
   ============================================================ */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html  { scroll-behavior: smooth; }
body  { background: var(--bg); color: var(--text); font-family: var(--font-body); line-height: 1.6; }
a     { text-decoration: none; color: inherit; }
ul    { list-style: none; }

/* ============================================================
   UTILITIES
   ============================================================ */
.mono   { font-family: var(--font-mono); }
.accent { color: var(--accent); }
.muted  { color: var(--muted); }

.container {
  max-width: var(--max-w);
  margin: 0 auto;
  padding: 0 24px;
}

.section-label {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  letter-spacing: 0.15em;
  color: var(--accent);
  margin-bottom: 2.5rem;
}

/* ============================================================
   BUTTONS
   ============================================================ */
.btn {
  display: inline-block;
  font-family: var(--font-mono);
  font-size: 0.8rem;
  padding: 0.5rem 1.2rem;
  border-radius: 3px;
  cursor: pointer;
  transition: var(--ease);
  border: none;
}
.btn-primary               { background: var(--accent); color: var(--bg); font-weight: 700; }
.btn-primary:hover         { background: #00cc6a; }
.btn-secondary             { background: transparent; color: var(--muted); border: 1px solid var(--border-dim); }
.btn-secondary:hover       { border-color: var(--accent); color: var(--accent); }

/* ============================================================
   TAGS
   ============================================================ */
.tag {
  display: inline-block;
  font-family: var(--font-mono);
  font-size: 0.7rem;
  padding: 2px 8px;
  border-radius: 2px;
  border: 1px solid;
}
.tag--primary   { border-color: var(--accent); color: var(--accent); background: var(--accent-dim); }
.tag--secondary { border-color: var(--border-dim); color: var(--muted); }

/* ============================================================
   NAVIGATION
   ============================================================ */
#nav {
  position: fixed;
  inset: 0 0 auto 0;
  height: var(--nav-h);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 24px;
  backdrop-filter: blur(10px);
  background: rgba(15, 15, 15, 0.85);
  border-bottom: 1px solid var(--border);
  z-index: 100;
}

.nav-brand            { font-family: var(--font-mono); font-size: 0.85rem; color: var(--muted); }
.nav-brand .mono      { color: var(--accent); }

.nav-links            { display: flex; gap: 1.5rem; }
.nav-links a          { font-family: var(--font-mono); font-size: 0.75rem; color: var(--muted); transition: color var(--ease); }
.nav-links a:hover,
.nav-links a.active   { color: var(--accent); }

.hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 4px;
}
.hamburger span {
  display: block;
  width: 22px;
  height: 1px;
  background: var(--muted);
  transition: transform var(--ease), opacity var(--ease);
}
.hamburger.open span:nth-child(1) { transform: translateY(6px) rotate(45deg); }
.hamburger.open span:nth-child(2) { opacity: 0; }
.hamburger.open span:nth-child(3) { transform: translateY(-6px) rotate(-45deg); }

.nav-overlay {
  display: none;
  position: fixed;
  inset: var(--nav-h) 0 0 0;
  background: var(--bg);
  z-index: 99;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2rem;
}
.nav-overlay.open { display: flex; }
.nav-overlay a    { font-family: var(--font-mono); font-size: 1.1rem; color: var(--muted); transition: color var(--ease); }
.nav-overlay a:hover { color: var(--accent); }

/* ============================================================
   SECTION BASE
   ============================================================ */
section          { padding: 6rem 0; }
section:not(#hero) { padding-top: calc(5rem + var(--nav-h)); }

/* ============================================================
   HERO
   ============================================================ */
#hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  overflow: hidden;
  padding: 0 24px;
}

#particles { position: absolute; inset: 0; z-index: 0; }

.hero-content {
  position: relative;
  z-index: 1;
  max-width: var(--max-w);
  width: 100%;
  margin: 0 auto;
  font-family: var(--font-mono);
}

.hero-prompt  { font-size: 0.85rem; color: var(--muted); margin-bottom: 0.3rem; }
.hero-status  { font-size: 0.8rem;  color: var(--muted); margin-bottom: 1.8rem; }

.hero-name {
  font-family: var(--font-body);
  font-size: clamp(2rem, 5vw, 3.5rem);
  font-weight: 800;
  letter-spacing: -0.03em;
  color: var(--text);
  margin-bottom: 0.4rem;
}

.hero-role {
  font-size: clamp(0.85rem, 2vw, 1.05rem);
  color: var(--accent);
  margin-bottom: 1rem;
  min-height: 1.8em;
}

.cursor { animation: blink 1s step-end infinite; }
@keyframes blink { 50% { opacity: 0; } }

.hero-tagline { font-size: 0.8rem; margin-bottom: 2.5rem; }

.hero-cta { display: flex; gap: 1rem; flex-wrap: wrap; }

/* ============================================================
   ABOUT
   ============================================================ */
.about-bio {
  font-size: 1rem;
  line-height: 1.85;
  max-width: 620px;
  color: var(--text);
  margin-bottom: 3rem;
}

.about-stats { display: flex; gap: 3rem; }

.stat          { display: flex; flex-direction: column; gap: 0.2rem; }
.stat-value    { font-family: var(--font-mono); font-size: 1.8rem; font-weight: 700; color: var(--accent); line-height: 1; }
.stat-label    { font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.1em; color: var(--muted2); }

/* ============================================================
   SKILLS
   ============================================================ */
.skills-grid   { display: flex; flex-direction: column; gap: 2rem; }

.skill-category {
  font-family: var(--font-mono);
  font-size: 0.65rem;
  letter-spacing: 0.15em;
  color: var(--muted2);
  margin-bottom: 0.6rem;
}

.skill-tags { display: flex; gap: 0.5rem; flex-wrap: wrap; }

/* ============================================================
   EXPERIENCE
   ============================================================ */
.timeline {
  position: relative;
  padding-left: 2rem;
  border-left: 1px solid var(--border-dim);
  display: flex;
  flex-direction: column;
  gap: 3rem;
}

.timeline-entry { position: relative; }

.timeline-node {
  position: absolute;
  left: calc(-2rem - 4px);
  top: 5px;
  width: 8px;
  height: 8px;
  border-radius: 50%;
  border: 1px solid var(--border-dim);
  background: var(--bg);
}
.timeline-entry--current .timeline-node {
  border-color: var(--accent);
  box-shadow: 0 0 8px var(--accent-dim);
}

.timeline-role { font-family: var(--font-mono); font-size: 0.9rem; font-weight: 600; color: var(--text); margin-bottom: 0.25rem; }
.timeline-entry--current .timeline-role { color: var(--accent); }

.timeline-date   { font-family: var(--font-mono); font-size: 0.75rem; color: var(--muted); margin-bottom: 0.8rem; }

.timeline-bullets { display: flex; flex-direction: column; gap: 0.4rem; }
.timeline-bullets li {
  font-size: 0.9rem;
  color: var(--muted);
  padding-left: 1.2rem;
  position: relative;
}
.timeline-bullets li::before { content: '›'; position: absolute; left: 0; color: var(--muted2); }

/* ============================================================
   PROJECTS
   ============================================================ */
.projects-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1.2rem;
}

.project-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 4px;
  padding: 1.4rem;
  transition: border-color var(--ease), box-shadow var(--ease);
  display: flex;
  flex-direction: column;
  gap: 0.8rem;
}
.project-card:hover { border-color: var(--accent); box-shadow: 0 0 14px var(--accent-glow); }

.project-name  { font-family: var(--font-mono); font-size: 0.9rem; font-weight: 600; color: var(--text); }
.project-desc  { font-size: 0.85rem; color: var(--muted); line-height: 1.65; flex: 1; }
.project-tags  { display: flex; gap: 0.4rem; flex-wrap: wrap; }

.project-link {
  font-family: var(--font-mono);
  font-size: 0.75rem;
  color: var(--muted2);
  border: 1px solid var(--border-dim);
  padding: 2px 10px;
  border-radius: 2px;
  display: inline-block;
  transition: color var(--ease), border-color var(--ease);
}
.project-link:hover { color: var(--accent); border-color: var(--accent); }

/* ============================================================
   CONTACT
   ============================================================ */
.contact-grid {
  display: grid;
  grid-template-columns: 1fr 1.2fr;
  gap: 3rem;
  align-items: start;
}

.contact-links { display: flex; flex-direction: column; gap: 1.4rem; }

.contact-link         { display: flex; flex-direction: column; gap: 0.2rem; }
.contact-link:hover .contact-url { color: var(--accent); }

.contact-label { font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.1em; color: var(--accent); }
.contact-url   { font-family: var(--font-mono); font-size: 0.8rem; color: var(--muted); transition: color var(--ease); }

.contact-form { display: flex; flex-direction: column; gap: 0.8rem; }

.form-input,
.form-textarea {
  width: 100%;
  background: var(--bg-input);
  border: 1px solid var(--border-dim);
  border-radius: 2px;
  padding: 0.6rem 0.8rem;
  color: var(--text);
  font-family: var(--font-mono);
  font-size: 0.8rem;
  transition: border-color var(--ease);
  outline: none;
}
.form-input::placeholder,
.form-textarea::placeholder { color: var(--muted2); }
.form-input:focus,
.form-textarea:focus { border-color: var(--accent); }
.form-textarea { resize: vertical; min-height: 100px; }

.form-status           { font-family: var(--font-mono); font-size: 0.75rem; min-height: 1.2em; }
.form-status--success  { color: var(--accent); }
.form-status--error    { color: #ff4444; }

/* ============================================================
   FOOTER
   ============================================================ */
footer { border-top: 1px solid var(--border); padding: 2rem 24px; text-align: center; }
footer p { font-family: var(--font-mono); font-size: 0.7rem; color: var(--muted2); }

/* ============================================================
   RESPONSIVE
   ============================================================ */
@media (max-width: 768px) {
  .nav-links   { display: none; }
  .hamburger   { display: flex; }

  section:not(#hero) { padding-top: calc(4rem + var(--nav-h)); }

  .about-stats   { gap: 1.5rem; }
  .projects-grid { grid-template-columns: 1fr; }
  .contact-grid  { grid-template-columns: 1fr; }
  .hero-cta      { flex-direction: column; align-items: flex-start; }
}
```

- [ ] **Step 2: Verify in browser**

```bash
python3 -m http.server 8080
```

Open `http://localhost:8080`. Expected:
- Dark background (`#0f0f0f`), green accent (`#00ff88`) on labels and stats
- Fixed nav at top, links visible on desktop
- Hero fills viewport with terminal prompt text and two CTA buttons
- All sections spaced correctly, timeline visible, project cards with borders, contact form styled
- Resize to 375px wide: nav links disappear, hamburger button appears

- [ ] **Step 3: Run sections tests**

```bash
npm test -- tests/e2e/sections.spec.js
```

Expected: All 5 pass.

- [ ] **Step 4: Commit**

```bash
git add assets/css/style.css
git commit -m "feat: add complete CSS — tokens, layout, components, responsive"
```

---

### Task 4: JS — tsParticles + Typed.js

**Files:**
- Modify: `assets/js/main.js`

- [ ] **Step 1: Replace `assets/js/main.js` with particle + typewriter code**

```js
// ============================================================
// PARTICLES — animated network background in hero
// ============================================================
tsParticles.load('particles', {
  background: { color: { value: 'transparent' } },
  particles: {
    number: { value: 60, density: { enable: true, area: 800 } },
    color: { value: '#00ff88' },
    opacity: { value: 0.35 },
    size: { value: { min: 1, max: 2 } },
    links: {
      enable: true,
      color: '#00ff88',
      opacity: 0.1,
      distance: 130,
    },
    move: { enable: true, speed: 0.6, outModes: 'bounce' },
  },
  interactivity: {
    events: { onHover: { enable: true, mode: 'grab' } },
    modes: { grab: { distance: 150, links: { opacity: 0.25 } } },
  },
  detectRetina: true,
});

// ============================================================
// TYPEWRITER — role title cycles through three strings
// ============================================================
new Typed('#typed', {
  strings: ['DevOps Engineer', 'Platform Engineer', 'Systems Developer'],
  typeSpeed: 60,
  backSpeed: 30,
  backDelay: 2000,
  loop: true,
  showCursor: false,
});
```

- [ ] **Step 2: Verify in browser**

Open `http://localhost:8080`. Expected:
- Green particle network animates across the hero background
- Moving the mouse over the hero causes nearby particles to connect with brighter lines
- Role title types out, pauses, erases, and loops through all three strings
- Blinking `_` cursor from HTML is visible while Typed.js is running

- [ ] **Step 3: Commit**

```bash
git add assets/js/main.js
git commit -m "feat: add tsParticles network background and Typed.js typewriter"
```

---

### Task 5: JS — GSAP ScrollTrigger animations

**Files:**
- Modify: `assets/js/main.js`

- [ ] **Step 1: Append GSAP animations to end of `assets/js/main.js`**

```js
// ============================================================
// GSAP SCROLL ANIMATIONS
// ============================================================
gsap.registerPlugin(ScrollTrigger);

gsap.from('#about .about-bio', {
  scrollTrigger: { trigger: '#about', start: 'top 80%' },
  opacity: 0, y: 24, duration: 0.7, ease: 'power2.out',
});

gsap.from('#about .stat', {
  scrollTrigger: { trigger: '#about .about-stats', start: 'top 85%' },
  opacity: 0, y: 16, stagger: 0.1, duration: 0.5, ease: 'power2.out',
});

gsap.from('#skills .skill-group', {
  scrollTrigger: { trigger: '#skills', start: 'top 80%' },
  opacity: 0, y: 16, stagger: 0.12, duration: 0.5, ease: 'power2.out',
});

gsap.from('#experience .timeline-entry', {
  scrollTrigger: { trigger: '#experience', start: 'top 80%' },
  opacity: 0, x: -24, stagger: 0.15, duration: 0.6, ease: 'power2.out',
});

gsap.from('#projects .project-card', {
  scrollTrigger: { trigger: '#projects', start: 'top 80%' },
  opacity: 0, y: 20, stagger: 0.1, duration: 0.5, ease: 'power2.out',
});

gsap.from('#contact .contact-grid', {
  scrollTrigger: { trigger: '#contact', start: 'top 80%' },
  opacity: 0, y: 20, duration: 0.7, ease: 'power2.out',
});
```

- [ ] **Step 2: Verify in browser**

Scroll slowly from the top. Expected:
- About bio fades up as it enters view; stats stagger in one by one
- Skill groups fade up in sequence
- Experience entries slide in from the left, staggered
- Project cards fade up in sequence
- Contact section fades up as a unit
- No content is permanently invisible (scroll back up, reload, scroll again)

- [ ] **Step 3: Commit**

```bash
git add assets/js/main.js
git commit -m "feat: add GSAP ScrollTrigger scroll-reveal animations"
```

---

### Task 6: JS — ScrollSpy + hamburger menu

**Files:**
- Modify: `assets/js/main.js`

- [ ] **Step 1: Append ScrollSpy and hamburger logic to end of `assets/js/main.js`**

```js
// ============================================================
// SCROLLSPY — highlight active nav link as user scrolls
// ============================================================
const sections = document.querySelectorAll('section[id]');
const navLinks = document.querySelectorAll('.nav-links a');

const scrollSpy = new IntersectionObserver(
  (entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        navLinks.forEach((link) =>
          link.classList.toggle('active', link.getAttribute('href') === `#${entry.target.id}`)
        );
      }
    });
  },
  { rootMargin: '-40% 0px -55% 0px' }
);

sections.forEach((section) => scrollSpy.observe(section));

// ============================================================
// HAMBURGER MENU
// ============================================================
const hamburger = document.querySelector('.hamburger');
const overlay   = document.querySelector('.nav-overlay');

hamburger.addEventListener('click', () => {
  const open = hamburger.classList.toggle('open');
  hamburger.setAttribute('aria-expanded', String(open));
  overlay.classList.toggle('open', open);
});

document.querySelectorAll('.nav-overlay a').forEach((link) => {
  link.addEventListener('click', () => {
    hamburger.classList.remove('open');
    hamburger.setAttribute('aria-expanded', 'false');
    overlay.classList.remove('open');
  });
});
```

- [ ] **Step 2: Run nav tests**

```bash
npm test -- tests/e2e/nav.spec.js
```

Expected: All 4 nav tests pass.

- [ ] **Step 3: Commit**

```bash
git add assets/js/main.js
git commit -m "feat: add ScrollSpy active nav highlighting and hamburger menu"
```

---

### Task 7: JS — Formspree contact form

**Files:**
- Modify: `assets/js/main.js`

- [ ] **Step 1: Append form handler to end of `assets/js/main.js`**

```js
// ============================================================
// CONTACT FORM — async Formspree submission
// ============================================================
const form   = document.querySelector('.contact-form');
const status = document.querySelector('.form-status');

form.addEventListener('submit', async (e) => {
  e.preventDefault();
  status.textContent = 'sending...';
  status.className = 'form-status';

  try {
    const res = await fetch(form.action, {
      method: 'POST',
      body: new FormData(form),
      headers: { Accept: 'application/json' },
    });

    if (res.ok) {
      status.textContent = 'done. message sent.';
      status.className = 'form-status form-status--success';
      form.reset();
    } else {
      throw new Error('non-ok response');
    }
  } catch {
    status.textContent = 'something went wrong. try emailing directly.';
    status.className = 'form-status form-status--error';
  }
});
```

- [ ] **Step 2: Run contact tests**

```bash
npm test -- tests/e2e/contact.spec.js
```

Expected: All 3 contact tests pass.

- [ ] **Step 3: Commit**

```bash
git add assets/js/main.js
git commit -m "feat: add async Formspree contact form handler"
```

---

### Task 8: Full test suite + manual QA

- [ ] **Step 1: Run all tests**

```bash
npm test
```

Expected: All tests in all three spec files pass. Output ends with something like:
```
13 passed (Xs)
```

- [ ] **Step 2: Manual browser pass**

Open `http://localhost:8080` and check each of the following:

- [ ] Hero particles animate, mouse hover brightens nearby links
- [ ] Typewriter cycles through all three role strings
- [ ] Blinking `_` cursor visible next to typed text
- [ ] Scroll down: each section's content animates in (bio, stats, skill groups, timeline, cards, contact)
- [ ] Active nav link turns green as you scroll into each section
- [ ] Resize to 375px: hamburger appears, nav links hidden
- [ ] Tap hamburger: overlay opens with links, tap again: closes
- [ ] Tap an overlay link: menu closes and page scrolls to section
- [ ] Hover over a project card: border turns green with glow
- [ ] Click a form field: border turns green
- [ ] No console errors

- [ ] **Step 3: Commit any fixes found during QA**

```bash
git add assets/css/style.css assets/js/main.js
git commit -m "fix: QA adjustments from manual browser pass"
```

(Skip this commit if nothing needed fixing.)

---

### Task 9: Personalise content

Update real data directly in `index.html`. No code logic changes.

- [ ] **Step 1: Replace all placeholder content**

| Location in `index.html` | Replace with |
|---|---|
| `<title>Your Name — DevOps Engineer</title>` | Your actual name |
| `nav-brand` text (`username`) | Your GitHub username |
| `h1.hero-name` | Your actual name |
| `hero-tagline` comment | Your personal tagline |
| About bio `<p>` | 2–4 sentences about yourself |
| Stat values (`3+`, `10+`, `5+`) | Your real numbers |
| All `<span class="tag ...">` contents | Your actual skill stack |
| Timeline entries | Your real roles, companies, dates, bullets |
| Project cards | Your real projects, descriptions, tags, GitHub links |
| Contact link `href` and URL text | Your real GitHub, LinkedIn, email |
| `mailto:you@email.com` | Your real email |

- [ ] **Step 2: Handle the CV button**

If you have a CV ready, place it at `assets/resume.pdf`.

If not, remove the download button from the hero in `index.html`:

```html
<!-- Remove this line: -->
<a href="assets/resume.pdf" download class="btn btn-secondary">cat resume.pdf</a>
```

- [ ] **Step 3: Commit**

```bash
git add index.html
git commit -m "content: add personal info, experience, projects, and contact links"
```

---

### Task 10: Deploy to GitHub Pages + custom domain

- [ ] **Step 1: Register your Formspree endpoint**

1. Go to https://formspree.io → sign up
2. Create a new form → copy the 8-character form ID (e.g. `xabcdefg`)
3. In `index.html`, replace `FORM_ID`:

```html
<!-- before -->
<form class="contact-form" action="https://formspree.io/f/FORM_ID" method="POST">

<!-- after (use your real ID) -->
<form class="contact-form" action="https://formspree.io/f/xabcdefg" method="POST">
```

4. Commit:

```bash
git add index.html
git commit -m "config: wire Formspree endpoint"
```

- [ ] **Step 2: Push to GitHub**

Create a repo on GitHub named `<your-github-username>.github.io`, then:

```bash
git remote add origin https://github.com/<username>/<username>.github.io.git
git branch -M main
git push -u origin main
```

- [ ] **Step 3: Enable GitHub Pages**

In the repo on GitHub → Settings → Pages → Source: Deploy from a branch → Branch: `main`, directory: `/` (root) → Save.

Wait ~60 seconds. Your site is live at `https://<username>.github.io`.

- [ ] **Step 4: Claim free .me domain via GitHub Student Pack**

1. Go to https://education.github.com/pack → find Namecheap → claim your free `.me` domain
2. In Namecheap DNS dashboard, add these records:

| Type | Host | Value |
|---|---|---|
| A | @ | 185.199.108.153 |
| A | @ | 185.199.109.153 |
| A | @ | 185.199.110.153 |
| A | @ | 185.199.111.153 |
| CNAME | www | `<username>.github.io` |

3. Create `CNAME` file in the repo root containing only your domain:

```
yourdomain.me
```

4. Commit and push:

```bash
git add CNAME
git commit -m "config: add custom domain CNAME for GitHub Pages"
git push
```

5. In GitHub → Settings → Pages → Custom domain → enter `yourdomain.me` → Save → tick "Enforce HTTPS"

- [ ] **Step 5: Verify (after DNS propagates — up to 1h)**

- Open `https://yourdomain.me` — site loads with correct content
- HTTPS padlock is visible
- All animations, nav, and contact form work correctly
- Send a test message through the contact form — check it arrives in Formspree dashboard

---

## Self-Review Notes

- All spec requirements covered: hero, about, skills, experience, projects, contact, nav, responsive, particles, typewriter, GSAP, Formspree, GitHub Pages, Namecheap
- No emojis anywhere — project names use `>` prefix per spec
- No proficiency bars in skills — tag badges only per spec
- `form-status--success` / `form-status--error` class names match between CSS (Task 3), JS (Task 7), and tests (Task 2) exactly
- `#particles`, `#typed`, `.nav-overlay`, `.hamburger` referenced consistently across HTML, CSS, and JS
- Placeholder content (bio, experience, projects) handled in Task 9 with explicit field-by-field instructions
- `FORM_ID` handled in Task 10 Step 1 with exact before/after HTML
